# Frame Extractor

    git clone https://github.com/kaspars-strods/frame-extractor
    cd frame-extractor
    npm install
    npm build
    npm run

Open http://localhost:5000 to view it in the browser.
